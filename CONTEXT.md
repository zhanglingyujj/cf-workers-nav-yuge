# 术语表

本项目为单上下文（Single-context）仓库，本文件只做词汇表，不含实现细节。

## 卡片形态

- **详情卡（info 卡）**：默认（非 APP 布局）分类下的卡片。横排结构：图标槽居左，名称与描述在右侧（描述最多两行省略）。对应 Sun-Panel 的 `info` 风格。
- **极简卡（icon 卡 / APP 布局卡）**：开启 APP 布局的分类下的卡片。竖排结构：图标居中在上、名称居中在下，不显示描述（描述仅作悬停提示）。对应 Sun-Panel 的 `icon` 风格。
- **APP 布局**：分类级开关，开启后该分类的卡片以极简卡渲染、网格列宽按极简卡规格。

## 导航结构

- **浮岛（Island）**：固定顶部的居中玻璃条，集合站点标题、搜索触发与设置入口，宽度与内容区卡片边界对齐。页面唯一常驻导航控件。
- **命令面板（Command Palette）**：浮岛搜索触发（或 `Alt C` / `/`）唤起的亮色面板，承担分类跳转、站点直达与多引擎搜索；分类导航的唯一入口（原快捷分类栏已删除，见 ADR-0002）。
- **清除筛选浮标（Filter Chip）**：站内搜索态下右上角的亮色浮标，提示当前筛选词，点击恢复全量视图。

## 数据变更

- **提交（commit）**：一次导航数据变更落库的动作单元；除站点设置（标题/壁纸）外的全部增删改/排序/分组持久化都经由它。_Avoid_：保存（泛指整体动作）、saveDataToServer（实现名）。
- **会话（Session）**：登录态的生命周期单元，接口为 `login(password)` / `logout()` / `load()` 三个动作（auth.js 承载）；token 存取、加载遮罩与链接数据的联动细节藏在其后。_Avoid_：把登录逻辑散落在弹窗或按钮处理器中各自实现。

## 视觉基线

- **深色内容 + 亮色浮层**：唯一主题。内容区（卡片/浮岛/区块控件）为深色玻璃，浮层（菜单/面板/弹窗/tooltip）为亮色白底，双层分明（ADR-0002）。
- **卡片背景色（backgroundColor）**：卡片级可选属性；留空即默认玻璃底。文字颜色按背景亮度自动黑白。
- **黑白灰配色**：无彩色强调色。暗面强调用白，亮面强调用近黑（zinc 阶）；黑底白字仅用于持久/键盘选中态。
